# Phase 0 — Tool survey (2026-09-13)

Platform: Windows 11, Python 3.12.10 (uv-managed venvs). The machine default is Python
3.14.3; it was **not** used because Cantera's wheel metadata caps at `<3.15` and several
candidates (OpenMDAO, CoolProp, turboflow `<4.0,>=3.11`) have no proven 3.14 support.
3.12 was the newest interpreter with wheels for every candidate. The 3.12 build on this
machine has no `tkinter`, which surfaced one packaging bug (see turbo-design).

Every status below is from an actual install + import + functional run in this session.
Smoke-test scripts: `scripts/phase0_tools/smoke_*.py`. Frozen envs: `requirements-np2.txt`,
`requirements-np1.txt`.

## 1. Summary table

| Candidate (brief name) | What it actually is | Install | Functional check | Verdict / role |
|---|---|---|---|---|
| **NASA pyCycle** | `om-pycycle` 4.4.1.dev0 from `git+https://github.com/OpenMDAO/pyCycle` @ ee7e161 (2026-05-20). **The PyPI package `pycycle` 0.0.8 is an unrelated circular-import linter** (installed, identified, removed). | OK (git) | Single-spool turbojet example: design point converged (Fn 11800 lbf target met, TSFC 0.7985, W 147.3 lbm/s, OPR 13.5) plus 2 off-design points (Fn 11000 lbf, Nmech 7944 rpm). Upstream example's print viewer crashes under NumPy 2 (`only 0-dimensional arrays...`); the solver is unaffected. | **ADOPT: Phase 3 cycle tool.** Has built-in CEA chemical-equilibrium thermo (JP-7 default, Jet-A tabular option), so real-gas combustion products are native rather than an ideal-gas assumption. |
| **Cantera** | 3.2.0 (PyPI wheel) | OK | `nDodecane_Reitz.yaml` (100 species) kerosene surrogate: phi=0.30 at T3=470 K, P3=4 bar gives T_ad 1230.9 K, gamma_products 1.3065, cp 1231.7 J/kg-K. ISA air cp 1001.8, gamma 1.4015. | **ADOPT: Phase 3.** Independent check of pyCycle's CEA thermo (T4, gamma, cp at turbine entry), flame-temperature / lean-limit envelope for combustor sizing, real-gas properties for turbomachinery hand checks. |
| **ADRpy** | 0.2.6 (PyPI, last release and commit 2024-01-26; upstream dormant, single tag) | OK only under NumPy 1.26 | Under NumPy 2: fails in `twrequired_to` and `liftslope_prad` (1-element array to scalar). Under NumPy 1.26 (`.venv-np1`): full jet (bpr=0) constraint diagram runs, 5 constraints x 14 wing loadings. | **ADOPT: Phase 2**, in the NumPy-1 env. Has `JetDeck` and a bpr/throttle-ratio thrust-lapse model appropriate to a turbojet. No mass-breakdown tool inside ADRpy, so the mass budget is a scripted statistical/parametric estimate (see gaps). |
| **pyAircraftEngineFramework** | `rmalpica/pyAircraftEngineFramework` (GitHub only, 0 stars, created 2026-01, Prosperity Public License = non-commercial). 176-line `TurbojetEngine.calculate(h, Ma)`: fixed component efficiencies, NASA-7 polynomials, on-design only. | cloned, inspected | No component maps, no off-design, no engine-deck export, no airframe coupling (grep for map/deck/airframe/mission: nothing). | **REJECT.** Duplicates pyCycle at lower fidelity and provides none of the engine-deck-to-airframe coupling implied by the brief. Coupling will be our own script: pyCycle off-design deck (thrust, SFC vs Mach/alt/throttle) feeding the ADRpy/mission script. |
| **TurboMAP** | Not found. PyPI: no package. GitHub: only unrelated hits (ORB-SLAM mapping, a C# object mapper, a 3-star C# "TurboMapCalculator" from 2021). | n/a | n/a | **NOT AVAILABLE.** Compressor-map generation is covered by TurboFlow / turbo-design (below), converted into pyCycle `MapData` format by a script we write. |
| **NASA PyTurbo** | = `pyturbo-aero` 1.3.8 (PyPI; GitHub `nasa/pyturbo-aero`, 91 stars, pushed 2026-09-01). **PyPI `pyturbo` 18.7.27 is an unrelated 2018 TensorFlow/OpenCV bundle**, not installed. | OK | Imports; `pyturbo.aero.Centrif` (centrifugal impeller + splitter builder), `Airfoil2D/3D`, `passage2D`. Geometry generation only, no performance prediction. | **HOLD for Phase 6** (3D blade/passage geometry after freeze). Not used for sizing. |
| **NASA Turbo-Design** | = `turbo-design` (GitHub `nasa/turbo-design`, 94 stars, pushed 2026-09-01). PyPI 1.4.2 (2026-03) is stale: stray `from turtle import ...` lines give `ModuleNotFoundError: tkinter`. Installed **GitHub main, v1.4.3 @ 23c2b0b** instead: bug gone, adds `turbodesign.centrifugal` (inducer/impeller/vaneless/vaned diffuser, Oh loss set, Wiesner slip), `operating_map`, `shaft_match`. | OK (git) | Ran the upstream NASA HECC validation fixture (NASA/CR-2014-218114 geometry, no tuned coefficients): work factor 0.8127 vs 0.8100 measured (+0.3 %), PR_tt 4.7805 vs 4.6847 (+2.0 %), eta_poly 0.8540 vs 0.8553 (-0.2 %). Speed line: `plots/phase0_turbodesign_hecc_validation.png`. Upstream states its own limits: choke flow 7-11 % high, vaned-diffuser diffusion factor outside correlation range, constant cp inside the solve. | **ADOPT: Phase 4 primary** for centrifugal compressor and axial turbine meanline (radial-equilibrium streamline solver; Ainley-Mathieson / Kacker-Okapuu / Craig-Cox turbine losses, Lieblein/OTAC compressor losses; Cantera-backed gas). |
| **TurboFlow** | `turboflow` 0.1.18 (PyPI; GitHub `turbo-sim/TurboFlow`, 56 stars, MIT, pushed 2026-05). Pins `numpy<2`, uses CoolProp. | OK, NumPy-1 env only | Centrifugal example (0.45 kg/s, 52 krpm, 143 mm impeller, Oh losses): converged in 3.4 s, eta_tt 83.8 %, PR_tt 2.664. Axial-turbine **design optimisation** (SLSQP; specific speed and blade-jet ratio as variables): converged in 99 s, eta_ts 90.8 %, PR_ts 2.298, full geometry returned. Performance-map mode exists for both machines. | **ADOPT: Phase 4 secondary**: (a) independent cross-check of turbo-design compressor numbers with a different loss set (Oh / Zhang), (b) axial-turbine design optimisation, (c) speed-line map generation for pyCycle off-design. Its example machine is close to our scale. |
| **TurboDesigner** | `turbodesigner` 2.0.0 (PyPI; GitHub `OpenOrion/turbodesigner`, 220 stars, MIT, pushed 2026-06). | OK | 2-stage axial compressor at 0.9 kg/s, PR 3, 70 krpm: runs, mean radius 58.7 mm, inlet Mach 0.45, stage PR 1.82 / 1.65. **Takes isentropic efficiency as an input**: free-vortex velocity triangles + Johnsen-Bullock metal angles + CadQuery STEP export, no loss model. Axial compressors only. | **HOLD**: geometry/CAD generator for an *axial* compressor option only. Used in Phase 3/4 only if the axial-compressor architecture survives the trade; otherwise Phase 6. Not a performance predictor. |
| numpy / scipy / matplotlib / pandas | 2.5.3 / 1.18.1 / 3.11 / 3.0.5 (main); 1.26.4 / 1.17.1 / 3.11 / 2.3.3 (np1) | OK | n/a | free use |

## 2. Environment decision: two venvs

`openmdao 3.45.1` requires `numpy>=2`; `turbodesigner 2.0.0` requires `numpy>=2.4.3`;
`turboflow` requires `numpy<2`; `ADRpy 0.2.6` breaks under NumPy 2 in at least two
independent code paths (take-off T/W and lift slope). Patching ADRpy line by line was
tried (the first fix exposed a second failure) and abandoned as unbounded. Isolating the
two NumPy-1 tools in `.venv-np1` costs nothing: the Phase 2 (ADRpy) and Phase 4
cross-check (TurboFlow) steps exchange only tabulated numbers (CSV/JSON) with the cycle model.

| env | packages | phases |
|---|---|---|
| `.venv` (NumPy 2.5) | openmdao, om-pycycle (git), cantera, turbo-design (git), turbodesigner, pyturbo-aero | 3, 4, 6 |
| `.venv-np1` (NumPy 1.26) | ADRpy, turboflow, CoolProp | 2, 4 (cross-check) |

## 3. Gaps and fallback plans

| Gap | Why it matters | Plan |
|---|---|---|
| **No centrifugal compressor map ships with pyCycle.** Built-in maps are NPSS large-engine maps: `AXI5` (axial, PR 5.2), `HPCMap` (E3-class, PR 10.9), `LPCMap`, `FanMap`, `NCP01` (a *fan*-scale map, Wc 1100-2100 lbm/s). None is a small centrifugal characteristic. | Off-design (Phase 3) needs a map whose shape (surge line, choke, efficiency islands) matches a small backswept centrifugal stage. | Phase 4 generates a speed-line map with turbo-design `operating_map` and/or TurboFlow performance-map mode; a converter script writes it into pyCycle `MapData` (Nc, R-line, Wc, PR, eta arrays). Until then, Phase 3 on-design and first-pass off-design use pyCycle's scaled `AXI5` / `HPT1269` maps, explicitly flagged as placeholders. |
| **No tool estimates engine mass or envelope.** | Engine mass is the largest single driver of the 25 kg closure. | Two independent estimates, both logged: (1) bottom-up from Phase 4 geometry (impeller, diffuser, turbine disc, shaft, combustor liner, casing volumes x material densities, plus bearings/starter/ECU/fuel pump from vendor data sheets); (2) statistical fit of thrust vs mass and diameter for commercial micro-turbojets in the 100-1000 N class, compiled from published data sheets into `data/microturbojet_database.csv` with a source per row. Any discrepancy between (1) and (2) is reported, not averaged away. |
| **No airframe mass-breakdown tool** (ADRpy has none). | Phase 2 mass budget. | Scripted parametric breakdown for small jet UAVs (structure fraction, systems, fuel, payload) using published small-UAV weight-fraction data (Raymer Ch. 3; Gundlach, *Designing Unmanned Aircraft Systems*), cited in the log, with fuel mass from pyCycle SFC x mission integration. Sensitivities reported. |
| **Engine deck to airframe coupling** (pyAircraftEngineFramework rejected). | Mission fuel and thrust lapse across the Phase 1 profile. | Own script: pyCycle off-design runs on a (Mach, altitude, throttle) grid -> `data/engine_deck.csv` -> mission integrator feeding ADRpy weight fractions. |
| **ADRpy dormant since 2024-01.** | Risk of further NumPy/SciPy breakage. | Pinned in `requirements-np1.txt`; the constraint equations are simple enough that a from-scratch reimplementation (Raymer / Gudmundsson forms) is the fallback if it breaks. |
| **turbo-design is pre-release and under active development** (GitHub main pinned to commit 23c2b0b; PyPI stale). Upstream lists unresolved choke-flow and diffuser-loss limitations. | Phase 4 compressor numbers. | Pinned commit; every result cross-checked against TurboFlow (independent loss models) and read against the HECC validation error band (eta +-0.5 pt, PR +-2 %, choke +7-11 %). Choke margin will be taken from TurboFlow as well before trusting either. |
| **pyCycle example code is NumPy-2-fragile** (viewer/print helpers). | Cosmetic. | Our scripts read outputs with `prob.get_val()` and index `[0]`; `OPENMDAO_REPORTS=0`. |

## 4. Preliminary observation to carry into Phase 1

From the ADRpy smoke run (placeholder inputs, not a result): with generic small-jet
aerodynamics (AR 6, CDmin 0.030, 60 m ground run, 1500 fpm climb, 150 kt cruise at 1 km,
2 g turn) the constraint diagram asks for T/W of about 0.3-0.6 over W/S 150-800 Pa,
while 500 N on 25 kg is T/W = 2.04. Either the mission is far more demanding
(high-subsonic dash, steep climb, high altitude, very short launch) or the 500 N reference
point is not the rating at which the 25 kg airframe is sized. This is the load-bearing
question for Phase 1 and is flagged there rather than resolved here.

## 5. Phase 2 additions (2026-09-13)

| Tool | Status | Role |
|---|---|---|
| **AeroSandbox** 4.2.10 (`.venv`, PyPI, MIT) | installed; `approximate_CD_wave`, `critical_mach`, `fuselage_base_drag_coefficient`, `sears_haack_drag_from_volume` verified or traced to cited sources | cited transonic helper models inside our drag build-up. `sears_haack_drag(radius, length)` returns CD on frontal area although documented as drag area (39x off in the test): not used. |
| **ADRpy** 0.2.6 | two more defects found in use: default quarter-chord sweep has an operator-precedence bug (`constraintanalysis.py:392`, lift slope 1.23 instead of 2.87/rad for our wing); the sustained-turn constraint uses the climb weight fraction | both worked around (explicit `sweep_25_deg`, one concept object per constraint); results cross-checked against DATCOM and our own mission model (dash drag 361.1 vs 361.3 N) |
| own slender-body wave-drag integral (`scripts/phase2_airframe/aero_utils.py`) | verified: Sears-Haack ratio 1.0007, parabolic-area body 0.9607 = exact analytic | shaping comparisons of the area distribution |

## 6. Phase 3 additions and findings (2026-09-13)

| Tool | Finding in use | Handling |
|---|---|---|
| NASA turbo-design (axial compressor) | no usable loss model: `loss/compressor/lieblein.py` is empty, OTAC classes are zero-returning placeholders, `DiffusionLoss` is an ad-hoc ramp | not used for axial efficiency |
| TurboDesigner 2.0.0 | efficiency is an input; rotor diffusion factor computed with absolute velocities (wrong); last stator `next_flow_station` asserts | used for geometry and triangles only; DF recomputed; own Howell loss model (verified) supplies efficiency |
| TurboFlow 0.1.18 (turbine design optimisation) | ignores the operating-point omega (derives it from specific speed); no structural constraint (optimum at 688 m/s tip speed) | omega equality constraint + blade-root-stress tip-radius constraint added |
| TurboFlow 0.1.18 (centrifugal performance) | works; needs its Latin-hypercube heuristic initial guess; the design point can be choked with a 0.5 throat ratio | throat opened until unchoked |
| pyCycle | TABULAR thermo agrees with Cantera to 0.3 % on T4; fresh-start design solves can fail at high OPR / low efficiency | retry from several initial guesses |
| pypdf (added to `.venv`) | used only to read the Cranfield combustion lecture PDF for the theta-parameter definition | not part of the design chain |

## 7. Phase 4 additions (2026-09-13)

| Tool | Status | Role |
|---|---|---|
| **scikit-fem** 12.0.2 (`.venv`, PyPI, BSD-3) | installed. Own axisymmetric rotating-body FE (`scripts/phase4_turbomachinery/axisym_fe.py`) verified against the Timoshenko & Goodier rotating-disc and thermal-disc solutions to within 0.5 %. Morley plate element verified against cantilever theory (+5 %) | impeller disc stress, burst, blade-root bending (impeller stress gate) |
| pypdfium2 5.13.0 (`.venv`) | used only to render data-sheet chart pages (ATI Ti-6Al-4V) for reading | not part of the design chain |
| gap | no approved tool does 3D solid FE of a bladed rotor (rake and lean, blade modes) | out of scope for the gate. Flagged in `docs/phase4_impeller_stress_gate.md` |
| **ross-rotordynamics** 2.3.0 (`.venv`, PyPI, MIT) | installed. Verified against the simply supported shaft, the Jeffcott rotor and an overhung gyroscopic disc (errors of 0.0001-0.20 %). Needs two import work-arounds for plotly 7 and numba 0.67 (`ross_shim.py`); these do not touch the FE matrices | rotor critical speeds, Campbell diagram, damped AF (`axial/docs/phase4_rotordynamics.md`) |
| TurboFlow 0.1.18 (turbine performance analysis) | two defects: (1) with `stop_on_failure=False`, a failed point crashes the summary printer (np.mean over None); (2) every point after a failed one fails, because the failed solution is reused as the initial guess | summary disabled; each speed line swept outward from the design PR (`turbine_map.py`) |
| TurboDesigner 2.0.0 (input convention) | `physical_area = flow_area * (1 + blockage)`. **Phase 3 passed 0.98 / 0.96 (a project error, not a tool bug), which doubled every axial annulus** | corrected to 0.02 / 0.04; axial trade re-run (`docs/phase3_engine.md` section 14) |
| gap | no approved tool gives axial-compressor maps | own stage-stacking model `axial_offdesign.py` (Howell stall criterion), verified at the design point, bracketed with the NPSS AXI5 map shipped with pyCycle |

## 8. Phase 3R findings (2026-09-14)

| Tool | Finding | Handling |
|---|---|---|
| TurboFlow 0.1.18, centrifugal compressor | **defect:** `slip_model.get_slip_weisner` computes `np.sqrt(np.cos(theta_out))` with `theta_out` in degrees, while `flow_model.py` treats it as degrees (`math.tand`). The solved slip factor is 0.931 instead of 0.837 at -30 deg (+18 % PR at a given tip speed), and the model returns NaN at -10 / -40 deg. At the upstream example's -24.5 deg the error is only 6 %, so the Phase 0 smoke test did not reveal it | run-time patch `scripts/phase3_cycle/turboflow_fixes.py` (imported by `centrifugal_design.py`, `centrifugal_map.py`); evidence and verification `verify_turboflow_slip.py` (solved σ = Wiesner exactly after the patch) |
| TurboFlow 0.1.18, centrifugal loss set (Oh) | limitation: no shock loss term, so efficiency at high inducer relative Mach is not trustworthy. The impeller incidence loss uses the full inlet relative tangential velocity (literal Conrad form) | spool speed limited to the fielded inducer range (M1s,rel <= 1.33, `inducer_anchor.py`) |
| TurboFlow 0.1.18, vaned diffuser ('custom' loss) | insensitive to the diffuser radius ratio (0.0003 in η between R4/R2 1.35 and 1.45), so it cannot size the diffuser, which sets the engine diameter | R4/R2 1.35 kept; flagged for Phase 4 |
| TurboFlow 0.1.18, turbine design optimisation | returns `success=False` at feasible points (flow, speed and tip constraints met exactly) for several fielded turbines; at 85 000 rpm it cannot meet the fielded flow within the stress limit | constraints checked explicitly (`docs/phase3r_centrifugal.md` section 5) |
| pyCycle T4-limited off-design mode (speed balance) | does not converge with the TurboFlow maps | deck built in N mode with a secant search on speed for T4 = T4max (`cc_mission.py`) |
| NASA turbo-design `turbodesign.centrifugal` (GitHub main 23c2b0b) | code review (Phase 4): no degree/radian errors; Oh loss set with Aungier entrance-diffusion and head-loss corrections; splitters via Aungier Z_eff; builds a stage from meanline scalars (`flowpath_builder.build_flowpath`). Limitations: no shock loss, no surge or diffuser-stall criterion (choke raises exceptions); the vaned-diffuser loss is a Lieblein cascade correlation beyond its fitted range (no area-ratio or stall physics), so it cannot size the diffuser; the solver needs `x_le` strictly inside the path. Minor issues: the inducer choke loss uses sin where the package elsewhere uses cos (inactive on HECC); leakage uses Z_eff while clearance and mixing use Z_exit; the diffuser P0 loops can fail to converge silently | used as the independent design-point check of the Phase 3R impellers (`td_check.py`): PR within 1 %, η within 1.5 points of TurboFlow |
| **Phase 6 CAD toolchain** (separate `.venv-cad`, Python 3.12.10, NumPy 2.5.3, `requirements-cad.txt`) | **CadQuery 2.8.0 / OCCT 7.9.3 (cadquery-ocp)**, **gmsh 4.15.2**, **pyturbo-aero 1.3.8**. Verified with `scripts/phase6_cad/smoke_cad.py`: box volume exact and STEP round trip exact; B-spline surface fitted through a point grid on a cylinder patch reproduces the analytic area (+0.0000 %); loft of closed spline sections gives a valid solid of the analytic volume (+0.0000 %); gmsh tetra mesh of the STEP box sums to the exact volume. Quirks: the OCP bindings need `GeomAbs_Shape` enums, not integers; the process exits non-zero after the script completes (teardown), so do not rely on the exit code. build123d resolves too, but ships a different OCP build (novtk), so not in the same env | a separate venv so the frozen `.venv` is untouched |
| pyturbo-aero 1.3.8 `Centrif` (impeller geometry) | generates blade suction/pressure-side point clouds (cylindrical and Cartesian), splitters, full wheel and hub/shroud; **no file export or solids** (a kernel is needed). Defect: `Centrif.patterns` is a class-level list that `__init__` appends to, so every new instance adds another default pattern shared by all instances (reset it, or build one wheel per process) | geometry generator for the impeller blades in Phase 6 (used; section 9) |
| pyturbo-aero 1.3.8 `Centrif`, found building the impeller (Phase 6) | see section 9 | |
| peak-PR surge surrogate (both meanline tools) | **fails on NASA HECC** (measured): real surge at 95 % of design flow with PR still rising (design SMN 8.4 %); turbo-design's characteristic rises down to 61 % flow (surrogate >= 73 %). For vaned diffusers, surge is set by diffuser stall, which neither tool models | surge margins are unvalidated; carried as risk R4R.1 (`hecc_surge_check.py`) |

## 9. Phase 6 findings (2026-09-14): CAD and 3D FE toolchain in use

| Tool | Finding | Handling |
|---|---|---|
| pyturbo-aero 1.3.8 `Centrif`, conventions | metal angles from meridional; a backswept TE has the same sign as the LE; thickness is a half-thickness in conformal units (t/2)/r; profiles are placed at i/(n-1) of span whatever `percent_span` says; `tip_clearance` is ignored; Cartesian y = r sin θ, z = r cos θ | one profile per span row; clearance put on the casing |
| pyturbo `Centrif`, defects | (1) the first thickness station receives the LAST list value; (2) `build()` cannot be called twice; (3) `apply_camber_shifts` loops over all profiles while only i+1 cambers exist (IndexError), so LE/TE theta shifts are unusable; (4) `add_SS_Wave` indexes a function instead of calling it; (5) `get_camber_length` is missing an argument; (6) `Centrif.patterns` is class-level (section 8) | (1) last value = the thin TE; (2) one build per process; (3)-(5) not used |
| pyturbo `Centrif`, camber | a 4-point Bezier per profile; with `use_mid_wrap_angle` every span gets the mid-span TE theta. With the short shroud this hooked the camber from 43 to 15 deg in the last 10 % of chord; the throat and the TE angle cannot both be met | `__build_camber__` hooked (not name-mangled: the name ends in `__`) with a least-squares Bezier of the radial-fibre + exducer-backsweep law (14 points, end slopes exact, fit <= 0.6 deg) |
| pyturbo `Centrif`, surfaces | SS and PS are Beziers whose CONTROL points are the camber offsets (not interpolated): with the default ~10 points the surfaces smooth away from camber +- t | `camber_follow_density = 60`: CAD angles within 1.1 deg of the law along the chord |
| pyturbo `Centrif`, splitters | `splitterblade` sits at the main blade's theta; the half-pitch offset is applied only in `__create_fullwheel__`. Taken as-is, every splitter lay inside a main blade (746 of 841 mm3 overlap), and the first wheel mass counted the splitters twice | splitter rotated +15 deg in theta; overlap checked = 0 |
| pyturbo `Centrif`, LE / TE | the LE is closed by a single nose point (30-67 deg kink between the first segments) and the TE by a perpendicular offset: a coarse closure, not a designed round | stated; no aero use of the LE shape |
| OCCT 7.9 / CadQuery 2.8, faceted solids | a blade solid sewn from triangles with FAN end caps (centroid fan of a thin cambered section) passes BRepCheck and has a plausible volume, but every boolean with it returned empty | ladder caps (SS[j]-PS[j] strips); grid-integrated volume matches the solid to 0.01-0.05 % |
| OCCT booleans | (a) a revolved wedge ∩ faceted blade returned empty where a planar prism worked; (b) the `common()` result of an UNCUT faceted solid, fused with the hub, added zero volume (one test); (c) fusing 24 faceted blades with the hub took > 17 min | (a) planar / ruled-loft tools only; (b) a volume-closure check after every boolean (the sector build asserts it); (c) wheel = 12 copies of a 30 deg sector |
| gmsh 4.15.2, `setPeriodic` | needs the same face topology on both cut surfaces ("Cannot find periodic counterpart of node"); the faceted blades crossing flat cut planes did not give it | cut faces meshed independently; cyclic tie by P2 interpolation on the master face (exact for matching meshes) |
| pyamg 5.3.0 (added to `.venv-cad`) | smoothed aggregation + CG converges in ~60 iterations on matching cyclic constraints (agrees with SuperLU to 1e-7); with the interpolated tie on the twisted boundary it stalled at 5e-4 after 2000 iterations, and at 3e-2 on the flat-cut sector with blade slivers (near-singular) | direct solvers instead. SciPy SuperLU (`splu`, `MMD_AT_PLUS_A`) is fine to ~150 k dof, but committed 13.7 GB (paging) at 220 k dof on this 14 GB machine. **pypardiso 0.4.7 (MKL PARDISO 2026.1)**, added to `.venv-cad`: agrees with SuperLU to 3e-15 (test matrix) and 5e-7 (verification B'), residual ~1e-12, 2.7 s vs minutes — the production solver |
| pyCycle off-design initial guesses (`cycle_model.set_design`) | fixed seeds (W 2.0 lbm/s, FAR 0.018, turbine PR 2.0) converge for the Phase 3/4 cycles, but at η_c 0.709 (W 3.0 lbm/s) Newton diverged to NaN and left every internal state (flow stations, map R-line, turbine map PR) at garbage, even with the Phase 4 maps | `cycle_model.seed_od_from_design` (copy the design-point implicit states) + marching from the design flight condition (Phase 4A) |
| pyCycle / OpenMDAO Newton, `od_mode="NT4"` sweeps | a failed solve keeps its diverged iterate as the next starting point: a 1150 → 700 K jump in imposed T4 failed, and every later point of the run then failed too (first `ax_transient.py` run: no converged point at any speed). With the down-march from 1150 K, 77.5 % speed still gave no converged point (cause not isolated) | `ax_transient.solve` restores the last converged output vector (`model._outputs.asarray(copy=True)` / `set_val`); T4 marched in 15 K steps from the steady line, speed marched along the steady line |
| own stage-stacking model (`axial_offdesign.py`) with fielded-level losses | scaling the whole calibrated row loss by the fielded debit doubles its incidence parabola and chokes every flow below 80 % speed; and a blading sized at a set efficiency must be rebuilt at that efficiency | `fielded()`: debit as a constant extra loss, triangles at `eta_sizing` (Phase 4A); the Phase 3 regression is unchanged |
| reportlab 5.0.1 (added to `.venv-cad`) | PDF generation for the engine specification sheet. Its standard Type-1 fonts lack Greek and math symbols (η, ≥, −), so the DejaVu TTFs that ship with matplotlib are registered. Output checked page by page after rasterising with pypdfium2 (throw-away `uv run --with pypdfium2`, not installed) | `spec_sheet.py` only; no analysis depends on it |
| TurboFlow 0.1.18, impeller throat | `area_throat_ratio` multiplies the EYE area; the choke check (`evaluate_throat`) is isentropic, relative flow normal to the throat, rothalpy = h01, throat at the arithmetic-mean eye radius. Reproduced by hand: tool level 0.70 -> 1.086, 0.75 -> 1.164 x design flow, the same bracket Phase 3R found | CAD throat checked with the same formula on the fielded eye (`impeller_cad.choke_ratio`) |
